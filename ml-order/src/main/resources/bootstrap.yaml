spring:
  application:
    name: ml-order
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 192.168.211.132:8848
        file-extension: yaml
        group: ml-group
        shared-configs:
          - data-id: common-config.yaml
            group: ml-group
            refresh: true

# 👇 👇 👇 关键：这里强制兜底，绝不允许为 null
rocketmq:
  name-server: 192.168.211.132:9876